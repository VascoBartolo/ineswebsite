# Secrets — injected at runtime (ACA secret) or mounted (docker-compose), never baked into the image
.env
credentials.json
token.json
token.pickle

# Python cruft
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.venv/
venv/

# VCS / docs / meta
.git
.gitignore
*.md
Dockerfile
.dockerignore

# macOS AppleDouble sidecars. Created whenever this repo is written to on an
# exFAT volume; the docker build context sender cannot read their xattrs and
# fails the whole build ("failed to xattr ._foo.py: operation not permitted").
# Both forms are needed: a bare `._*` only matches at the context root, so the
# `**/` variant is what catches them in subdirectories.
._*
**/._*
.DS_Store
**/.DS_Store
